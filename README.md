# 2026统计建模

这次的项目主要是关于数据开放平台对于企业创业环境(本文选取的是企业留言板上的企业诉求量指标)的一次因果实证。

---

## 项目流程

### 1. 数据
初始数据为`【0405】左连结数据整合 - 副本.xlsx`和`1_插值补充后数据.xlsx`,为全国各地各个省份的宏观指标以及政府诉求量。

### 2. 数据清洗
对于缺失值主要采用的是线性插值方法:

```python
df[target_cols] = df.groupby('地级市')[target_cols].apply(
    lambda x: x.interpolate(method='linear', limit_direction='both')
).reset_index(level=0, drop=True)
```

### 3. 特征工程
进行特征变量的筛选,主要选取的是人力资本水平等:

```python
df['人力资本水平'] = df['普通高等学校在校学生数(人)'] / df['户籍人口(万人)']
df['传统基础设施'] = np.log(df['公路货运量(万吨)'] + 1)
df['互联网发展水平'] = np.log(df['电信业务收入(万元)'] + 1)
df['金融发展水平'] = df['年末金融机构各项贷款余额(万元)'] / df['地区生产总值(万元)']
df['财政压力水平'] = df['地方财政一般预算内支出(万元)'] / df['地方财政一般预算内收入(万元)']
df['科学支出水平'] = np.log(df['科学支出(万元)'] + 1)
df['城市发展水平'] = np.log(df['人均地区生产总值(元)'])
df['外商投资水平'] = np.log(df['外商投资企业数(个)'])
df['交通便捷程度'] = df['高速公路里程(公里)'] / df['户籍人口(万人)']
```

### 4. DML基准回归
对于清洗好的数据使用DML进行基准回归分析,主要关注的是`post`这一个系数的正负(如果为正,则起到极好的效应,为负则为负效应,当然前提是显著)。采用`econml`包进行分析:

```python
from econml.dml import LinearDML
from sklearn.ensemble import RandomForestRegressor, RandomForestClassifier
est = LinearDML(model_y=RandomForestRegressor(n_estimators=100, random_state=42),
                model_t=RandomForestClassifier(n_estimators=100, random_state=42),
                discrete_treatment=True,
                cv=5,
                random_state=42)
```

### 5. 异质性分析
分省份分地区,看看经济不同的地方这个政策是不是都有效。→ `project.ipynb`

### 6. 中介效应分析(机制分析)
看看通过什么进行传导(本文选的是营商环境):
数据开放 → 营商环境变好 → 企业创业活力提升

主要采用的是温忠麟三步法,但我们这一次两步就解决了,只要系数都显著,就说明中介效应检验有效。→ `mediator.ipynb`

---

## Learned and Review

这一次项目学到了什么?

### 1. 因果推断方法(景仰已久)
用的是双重机器学习方法,这个方法实际理解起来很简单:就是先用X去解释post,剥离掉post里面能用X解释的部分,然后再用post和X都扔进预测Y的回归分析里,这样Y的残差就是纯净的Y,第一个分类算法里的post残差就是纯净的post,然后对于这两个残差进行回归就可以得出我们想要的系数了。

### 2. dml包和插值函数

```python
from econml.dml import LinearDML
df.dropna(subset=[cols])
df[target_cols] = df.groupby('地级市')[target_cols].apply(
    lambda x: x.interpolate(method='linear', limit_direction='both')
)
```

### 3. 非参数、半参数、参数统计方法

**非参数**: 不假定表达式形式,全靠数据自行去拟合(也就是使用核函数等、插值什么的,不假定这些变量是一个线性的什么的)

**半参数**: 假定一部分,就比如这个,只假定post是线性的,剩下的X用$g(X)$去拟合,$g()$为选的机器学习算法
$$
Y = \alpha * post + g(X) + \varepsilon, \quad \mathbb{E}(\varepsilon \mid D, X) = 0
$$

**参数**: 典型的就是线性回归,我就是假定你这些就是线性的,最终拟合出这样一个式子就行
$$
y = \beta +\beta_1 x_{1}+\beta_{2}x_{2}
$$

---

## 不足与反思

1. **内生性问题处理不够完善**: 在DML框架中,第一步剥离混杂因素、第二步残差回归这套流程在路径较短时(如直接T→Y)效果尚可,但当中介效应分析的步骤增多时(如T→M→Y的多步传导),每一阶段的估计误差会逐层累积。具体表现为后续中介效应估计中标准误急剧膨胀,导致系数不再显著,使得中介机制的可信度下降。核心原因在于DML第一阶段使用的是机器学习模型(如随机森林)来拟合nuisance functions,模型自身的方差会在多步传导中被层层放大。后续应考虑使用更稳健的双稳健估计方法(如DML with cross-fitting + debiased machine learning)或采用结构方程模型(SEM)来统一估计整个中介路径,而非分步估计再拼凑。

2. **对照组构建有待加强**: 平行趋势检验只对处理组做了事件研究,未严格验证处理组与对照组在政策前的变化趋势是否一致。

3. **政策时点异质性**: 各地开放数据平台的年份不同(2012-2024年),属于交错DID(Staggered DID)场景,传统TWFE估计量可能存在负权重问题,后续可考虑使用CSDID或Bacon-Decomposition进行稳健性检验。

---

## 项目全景概览

### 研究背景
数据要素作为新型生产要素,公共数据开放平台的建设能否真正激发企业创业活力?本项目以全国各地级市面板数据为基础,使用**双重机器学习(DML)**方法,实证检验公共数据开放对企业诉求量(创业活跃度的代理变量)的因果效应,并探索**营商环境**在其中发挥的中介传导作用。

### 数据管道

| 步骤 | 输入文件 | 处理脚本 | 输出文件 | 说明 |
|:---:|:---|:---|:---|:---|
| 数据清洗 | `【0405】左连结数据整合 - 副本.xlsx` | `【260406数据预处理可用】插值+基准回归+平行趋势检验.py` | `1_插值补充后数据.xlsx` | 按地级市分组线性插值填补缺失值 |
| 特征工程 | `1_插值补充后数据.xlsx` | 同上 | `2_处理后含控制变量数据.xlsx` | 构造9个控制变量(人力资本、基础设施等) |
| 基准回归 | `2_处理后含控制变量数据.xlsx` | 同上 | `3_DML基准回归结果.xlsx` | DML估计ATE,含城市+年份固定效应 |
| 平行趋势检验 | `2_处理后含控制变量数据.xlsx` | 同上 | `4_平行趋势检验结果.xlsx` | 事件研究法验证处理前趋势一致性 |
| 模型稳健性 | `2_处理后含控制变量数据.xlsx` | `【260406】DML得到ATE及显著性.py` | — | 6种ML模型(Lasso/Ridge/RF/GBDT/XGBoost/LightGBM)对比 |
| 多变量扩展 | `填补的数据整合 - 副本.xlsx` | `Y＋1取对，总＋分地区，时间固定效应.py` | — | 扩展至142个控制变量 + 省份-年份联合固定效应 |
| 异质性分析 | `填补的数据整合 - 副本.xlsx` | `project.ipynb` | — | 分省份/分地区估计、缩尾处理、剔除特殊样本 |
| 中介效应 | `mediate_merge_final.xlsx` | `mediator.ipynb` | — | 营商环境得分构建 + 温忠麟两步法中有效应检验 |
| 文本识别 | `原始留言数据.xlsx` | `【20260503】提示词工程.py` | — | 两阶段法(关键词粗筛+大模型精筛)识别民营企业诉求 |

### 项目文件树

```
2026 tjjm/
├── README.md                          # 项目说明
├── 作品.docx                           # 最终作品文档
├── 观察文献/                            # 参考文献
│   ├── 24国奖.pdf                       #   国奖参考作品
│   ├── 24福建.pdf                       #   省级参考作品
│   ├── 数据要素共享与城市创业活力...       #   核心参考: 蔡运坤
│   ├── 营商环境优化何以赋能...             #   核心参考: 王鸿
│   ├── 网络基础设施、包容性绿色增长...      #   核心参考: 张涛
│   ├── 中介效应分析_方法和模型发展...       #   方法论: 温忠麟
│   ├── 中介效应检验程序及其应用...          #   方法论: 温忠麟
│   ├── 因果推断经验研究中的中介效应与调节效应 #  方法论: 江艇
│   ├── 经济学实证研究中的稳健性检验...       #   方法论: 刘明
│   ├── 数字基础设施建设能否优化...          #   参考: 牛朝文
│   ├── 数字经济对区域绿色全要素生产率...     #   参考: 严芝清
│   └── "靶向执法"助力营商环境优化...        #   参考: 宋林霖
└── project/
    ├── 数据文件
    │   ├── 【0405】左连结数据整合 - 副本.xlsx   # 原始面板数据(36列)
    │   ├── 填补的数据整合 - 副本.xlsx            # 扩展版原始数据(153列)
    │   ├── 1_插值补充后数据.xlsx                 # 插值清洗后数据
    │   ├── 2_处理后含控制变量数据.xlsx            # 含构造控制变量的数据
    │   ├── 3_DML基准回归结果.xlsx                # DML基准回归输出
    │   ├── 4_平行趋势检验结果.xlsx                # 平行趋势检验输出
    │   ├── final.xlsx                            # 中介效应-宏观经济指标面板
    │   ├── mediate.xlsx / mediate_merge.xlsx     # 中介效应中间数据
    │   ├── mediate_merge_final.xlsx              # 含营商环境得分的最终数据
    │   ├── 中介变量.xlsx                          # 中介变量原始数据
    │   └── 导出详情单locate1.xlsx                # 企业留言明细数据
    ├── 核心脚本
    │   ├── 【260406数据预处理可用】插值+基准回归+平行趋势检验.py  #  数据清洗→特征工程→DML回归→平行趋势检验 一键管道
    │   ├── 【260406】DML得到ATE及显著性.py                     #  6模型稳健性对比 + 分地区异质性
    │   ├── Y＋1取对，总＋分地区，时间固定效应.py                    #  多变量DML + 省份-年份联合固定效应
    │   └── 【20260503】提示词工程.py                            #  两阶段文本识别(关键词+大模型)
    └── 分析笔记本
        ├── project.ipynb      #  异质性分析: 分省估计、稳健性检验(缩尾/剔除样本)、省份-年份固定效应
        └── mediator.ipynb     #  中介效应: 营商环境得分构建、路径A(T→M)+路径B(M→Y)+路径C(T→Y)估计
```

### 核心方法

$$
\small
\begin{aligned}
&\text{【基准回归】}\quad Y = \alpha \cdot \text{post} + g(X) + \varepsilon \\[4pt]
&\text{【中介效应 - 路径A】}\quad M = \gamma_1 \cdot \text{post} + g_1(X, W) + \varepsilon_1 \\[4pt]
&\text{【中介效应 - 路径B】}\quad Y = \beta_2 \cdot M + g_2(X, W, \text{post}) + \varepsilon_2 \\[4pt]
&\text{【中介效应 - 路径C】}\quad Y = \tau \cdot \text{post} + g_3(X, W) + \varepsilon_3 \\[4pt]
&\text{【间接效应】}\quad \text{Indirect} = \gamma_1 \times \beta_2
\end{aligned}
$$

### 关键技术点

| 技术 | 应用 |
|:---|:---|
| Linear DML (Double ML) | 基准因果效应估计,剥离高维控制变量的混杂影响 |
| Cross-fitting (5折) | 防止过拟合,保证估计量渐近正态 |
| 城市+年份双向固定效应 | 控制不随时间/地区变化的遗漏变量 |
| 事件研究法 (Event Study) | 平行趋势检验,验证DID有效性 |
| 多模型稳健性 (6种ML) | Lasso/Ridge/RF/GBDT/XGB/LGBM — 检验结果对模型选择不敏感 |
| 温忠麟两步法 | 中介效应检验:路径A显著+路径B显著→中介成立 |
| 熵权法/组合赋权 | 15个指标合成营商环境综合得分 |
| 缩尾处理 (1%/5%-95%/99%) | 消除异常值对估计结果的干扰 |
| 交错DID处理 | 针对各地政策时点不同进行异质性分析 |
| 提示词工程 + LLM | 两阶段文本分类:关键词粗筛→大模型精筛识别企业诉求 |
